# Branches
## BPG
Contains a cluster with BGP peering, Kubernetes Gateway, Cilium + Hubble (exposed via Gateway API and advertise service over BGP with Loadbalancer IP) and WireGuard enabled
## L2Announcement
Contains a cluster with L2Announcment (instead of using MetalLB), Kubernetes Gateway, Cilium + Hubble (exposed via Gateway API with Loadbalancer IP)
